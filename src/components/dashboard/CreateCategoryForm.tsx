import { CategoryInfo } from '@/models/Category'
import React from 'react'

const CreateCategoryForm = ({category}: {category?: CategoryInfo}) => {
  return (
    <form>
        CreateCategoryForm
    </form>
  )
}

export default CreateCategoryForm